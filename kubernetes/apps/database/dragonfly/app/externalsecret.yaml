---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &secret dragonfly-auth
spec:
  # 0 freezes the value: any interval re-runs the generator and rotates the
  # password under a running Dragonfly. It also makes edits to the template
  # below silently inert - delete the target Secret to reshape it, which mints a
  # new password.
  refreshInterval: "0"
  target:
    name: *secret
    template:
      data:
        password: "{{ .password }}"
  dataFrom:
    # generatorRef only works under dataFrom - it is unimplemented in data[].
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: ClusterGenerator
          name: password
