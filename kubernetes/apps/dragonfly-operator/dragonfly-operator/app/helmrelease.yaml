---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: dragonfly-operator
spec:
  chartRef:
    kind: OCIRepository
    name: dragonfly-operator
  interval: 1h
  values:
    crds:
      install: true
      keep: true
    manager:
      # Owns master election for the Dragonfly replicas; without it a failed
      # master is never promoted.
      priorityClassName: cluster-platform
      image:
        repository: ghcr.io/dragonflydb/operator
    rbacProxy:
      enabled: false
    serviceMonitor:
      enabled: true
    grafanaDashboard:
      enabled: false
